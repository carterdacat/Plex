/*Array.prototype.getUniqe = (array: Array<any>): Array<any> => {
    function x(value, index: number, self: any[]) {
        return self.indexOf(value) === index;
    }
    return array.filter(x);
};*/
